# U-Net 阅读笔记

U-Net 要解决的问题，是判断图像中的每个像素属于什么。

## 我对U-Net网络结构的理解

U-Net 的结构像字母 U：左边逐渐缩小图像特征，右边再逐渐放大。

左边通过卷积提取线索，再用池化缩小尺寸。右边通过上采样放大特征。并且U-Net 会通过跳跃连接将左边保留的细节传到右边。

## 网络是怎么学会分割的

​	网络先为每个像素给出判断，然后损失函数把预测与人工标注进行比较：**给正确答案的概率越低，损失就越大。**接下来，梯度下降按照损失最小的方向走一小步。反复训练后，网络逐渐改善。
